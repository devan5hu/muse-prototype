flask==2.0.1
flask-cors==3.0.10
Werkzeug==2.0.1
gunicorn==20.1.0

# Environment variables
python-dotenv

# HTTP requests
requests==2.31.0

# Image processing
Pillow

# Google Cloud services
google-cloud-aiplatform
vertexai

# AWS SDK (if you're using AWS services)
boto3==1.28.38
botocore==1.31.38

# File handling and utilities
python-magic
pydub

# JSON handling
jsonschema

# Date/time handling
python-dateutil

# For working with video files
moviepy
scikit-learn
twelvelabs

# Add Cohere client library
cohere

# Add Voyage AI client library
voyageai

# Data processing
numpy

# Update requirements.txt to remove invalid entry and add any missing dependencies

import os
import pickle
import json
import logging
import numpy as np
from flask import Blueprint, request
from app.services.azure_service import AzureService
from app.utils.helpers import save_uploaded_file, get_file_url, create_cors_response
from app.utils.s3_helper import upload_file_to_s3

# Remove the invalid 'pickle' entry - pickle is part of Python's standard library
# Make sure numpy is included for array operations